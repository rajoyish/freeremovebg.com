---
// English root (`/`). Renders the shared, dictionary-driven home page.
import HomePage from '../components/HomePage.astro';
import { DEFAULT_LANG } from '../i18n/config';
---

<HomePage lang={DEFAULT_LANG} />
